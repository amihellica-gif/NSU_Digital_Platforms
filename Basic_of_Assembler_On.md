# Конспект: Основы учебного ассемблера CdM-8

## 1. Базовые понятия
Ассемблер — это язык, на котором мы общаемся напрямую с процессором. Процессор не умеет считать переменные прямо в оперативной памяти. Ему нужно принести данные к себе, посчитать, и отнести обратно.

*   **Регистры (`r0`, `r1`, `r2`, `r3`)** — «карманы» процессора. Сверхбыстрая память, в которой происходят все вычисления.
*   **Оперативная память (RAM)** — долговременное хранилище (как шкафчики с номерками). 
*   **Адрес (например, `a`)** — «номерок» шкафчика.
*   **Значение (например, `mem[a]`)** — то, что лежит внутри этого шкафчика.

---

## 2. Главные команды (Мнемоники)

### Работа с памятью и регистрами
*   `ldi <rn>, <const>` (Load Immediate) — положить конкретное число или адрес напрямую в регистр.
    * *Пример:* `ldi r1, a` (положит адрес `a` в регистр `r1`).
*   `ld <rn>, <rm>` (Load) — ЧТЕНИЕ из памяти. Идет по адресу, который лежит в `rn`, берет оттуда данные и кладет в `rm`.
    * *Пример:* `ld r1, r1` (достанет значение по адресу из `r1` и перезапишет им сам `r1`).
*   `st <rn>, <rm>` (Store) — ЗАПИСЬ в память. Берет значение из `rm` и сохраняет его в оперативку по адресу, который лежит в `rn`.
    * *Пример:* `st r2, r1` (сохранит число из `r1` по адресу из `r2`).
*   `move <rn>, <rm>` — копирует значение из регистра `rn` в регистр `rm`.

### Математика (результат сохраняется во второй регистр)
*   `add <rn>, <rm>` — сложить два регистра (`rm = rn + rm`).
*   `sub <rn>, <rm>` — вычесть первый из второго (`rm = rm - rn`).
*   `inc <rn>` — увеличить значение регистра на 1 (Инкремент).
*   `dec <rn>` — уменьшить значение регистра на 1 (Декремент).
*   `neg <rn>` — умножить на -1 (поменять знак числа).
*   `clr <rn>` — обнулить регистр.

### Директивы компилятора (не выполняются процессором)
*   `dc <const>` (Define Constant) — положить данные (число, букву, строку) в оперативную память при запуске программы.

---

## 3. Золотые паттерны (Шпаргалка для лаб)

**Паттерн 1: Прочитать переменную из памяти (r1 = mem[a])**
```assembly
ldi r1, a    # 1. Берем адрес
ld r1, r1    # 2. Достаем по нему значение

**Паттерн 2: Записать число в память (mem[a] = 16)**
```assembly
ldi r1, 16   # 1. Готовим данные (что сохраняем)
ldi r2, a    # 2. Готовим адрес (куда сохраняем)
st r2, r1    # 3. Отправляем данные по адресу

**Паттерн 3: Скопировать из одной ячейки в другую (mem[b] = mem[a])**
```assembly
ldi r1, a    # 1. Берем адрес источника
ld r1, r1    # 2. Читаем данные из источника
ldi r2, b    # 3. Берем адрес получателя
st r2, r1    # 4. Записываем данные получателю

